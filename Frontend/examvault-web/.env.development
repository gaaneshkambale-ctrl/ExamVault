# Local dev default. No secrets here - contains only the local API base URL.
# Until Phase 2 (YARP Gateway) is wired up, the frontend talks to User API directly.
VITE_API_BASE_URL=http://localhost:5010
